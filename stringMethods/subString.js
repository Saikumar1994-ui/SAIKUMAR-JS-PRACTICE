let persons = "sai, kumar, gopi, praveen";
let pson = persons.substring(5, 10);
console.log(pson);

let pson1 = persons.substring(-5, -10);
console.log(pson1); // no return value

//If you omit the second parameter, substring() will slice out the rest of the string.
